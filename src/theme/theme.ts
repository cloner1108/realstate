"use client";

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  direction: "rtl", // Set the direction to RTL
  typography: {
    fontFamily: "Roboto, Arial, sans-serif", // Customize as needed
  },
});

export default theme;